{% extends "base.html.twig" %}

{% block title %}Administration{% endblock %}

{% block body %}

	<h1>{{ block('title') }}</h1>

	<div class="row">

		<div class="col-md-6">
			<div class="panel panel-default">
				<div class="panel-heading">Administration</div>
				<div class="panel-body">
					<p><a href="{{ path('admin_user_list') }}">Utilisateurs</a></p>
				</div>
			</div>
		</div>

		<div class="col-md-6">
			<p><a href="{{ path('user_profile') }}" class="btn btn-default">Profil du compte</a></p>
			<p><a href="{{ logout_path() }}" class="btn btn-default">Déconnexion</a></p>
		</div>

	</div>

{% endblock %}
